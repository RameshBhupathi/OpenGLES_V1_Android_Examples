<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ramesh.io.openglesexamples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".drawing_2d_shapes.Drawing2dShapes" />
        <activity android:name=".color.ColorsForShapesActivity" />
        <activity android:name=".rotation.RotationActivity" />
        <activity android:name=".shapes_3d.Shapes3dActivity" />
        <activity
            android:name=".texture_cube.TextureCubeActivity"
            android:label="@string/title_activity_texture_cube"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>